---
description: Blog 앱 FSD 아키텍처 규칙
globs:
  - apps/blog/**
---

## Feature-Sliced Design 구조

```
apps/blog/src/
├── app/        # 앱 레벨 (providers, analytics, seo)
├── entities/   # 도메인 엔티티 (post, spotify, social)
├── features/   # 기능 모듈 (switch-theme, spotify-polling)
├── widgets/    # 복합 UI (header, footer, post-card)
└── shared/     # 공유 유틸 (config, hooks, lib, ui)
```

## 모듈 구조

각 모듈은 다음 구조를 따름:

```
src/widgets/post-card/
├── __tests__/
│   └── post-card.test.tsx
├── ui/
│   └── PostCard.tsx      # PascalCase
└── index.ts              # barrel export
```

## 네이밍

- 폴더: kebab-case (`post-card`, `switch-theme`)
- `ui/` 내 컴포넌트 파일: **PascalCase** (`PostCard.tsx`)
- 테스트 파일: kebab-case (`post-card.test.tsx`)

## Import 규칙

- FSD 레이어 간 import: 위에서 아래로만 허용
  - `app` → `widgets` → `features` → `entities` → `shared`
- 같은 레이어 간 cross-import 금지

```typescript
// Good: widgets에서 entities 사용
import { Post } from '@/src/entities/post';

// Bad: entities에서 widgets 사용
import { PostCard } from '@/src/widgets/post-card';
```

## MDX Frontmatter 필수 필드

```yaml
---
title: '포스트 제목'
date: '2025-01-27'
description: '포스트 설명'
tags: ['tag1', 'tag2']
draft: false  # 생략시 false
---
```
