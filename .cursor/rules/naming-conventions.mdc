---
description: 파일 및 폴더 네이밍 컨벤션
alwaysApply: true
---

## 파일/폴더 네이밍

- 폴더명: **kebab-case** (`post-card`, `switch-theme`)
- 파일명: **kebab-case** (`counter.tsx`, `theme-provider.tsx`)
- 테스트 파일: `*.test.ts(x)` 또는 E2E는 `*.spec.ts`
- 설정 파일: kebab-case (`jest.config.mjs`, `vite.config.ts`)

## 테스트 폴더 구조

- 단위 테스트: 소스 코드와 colocate된 `__tests__/` 폴더
- E2E 테스트: 프로젝트 루트의 `e2e/` 폴더

```
src/
├── components/
│   └── counter.tsx
├── __tests__/
│   └── counter.test.tsx
e2e/
└── home.spec.ts
```
